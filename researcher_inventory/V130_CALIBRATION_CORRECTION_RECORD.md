# Researcher Inventory V130 Calibration Correction Record

Status: `PROSPECTIVE SUCCESSOR CORRECTION — CALIBRATION ONLY`  
Date: 2026-09-20  
Predecessor contract: `RI-CONTRACT-V129`  
Successor contract: `RI-CONTRACT-V130`  
Authority: Leah's standing Researcher Inventory calibration instruction  
Executive Registrar record: `APA-EXEC-2026-09-20-CASES-RI-V130-CAL-0001`

## Predecessor evidence

Workflow run `35557741743` executed V129 through the retained V126A contract-subordinate request harness. Immediately before semantic calibration, the workflow directly verified both immutable Leah-approved archetype workbook copies at the exact required SHA-256 values:

- `Case_2_Lightweight_Researcher_Inventory.xlsx` = `d47915552fdbdd23adc8c3f66ab06f94e022ab0bfd93faa5faabb53fdf9c9193`
- `Case_6_Lightweight_Researcher_Inventory_v2_with_Verbs_and_Locators.xlsx` = `50d646aad08b33051b93c1f3b3538d59ff78386558473b71a906a94213635070`

No workbook repair was required. All 24 deterministic apparatus/harness tests passed. A fresh saved agent was bootstrapped from the declared V129 contract. The paired hidden-archetype diagnostic then failed semantically on both materially different sources. The complete attempt/failure evidence was preserved under `calibration_history/researcher_inventory/35557741743-A1`; the sealed holdout and clean-room stages were not reached.

## Independent harness classification

No new harness defect is established by the V129 run. The immutable-hash gate, V64 compound-order tests, V66 field-aware evaluator correction, V88A editor-shorthand alignment correction, V126A contract-subordinate prompt tests, apparatus serialization checks, saved-agent bootstrap, source-span enforcement, candidate-history preservation, and holdout gating all satisfied their deterministic preconditions before the semantic mismatch.

The defect classification is therefore:

`WORKER-SEMANTIC / CONTRACT-GENERALIZATION DEFECT`

The existing V126A harness remains unchanged.

## Generalizable V129 defects

### 1. The independent-card threshold over-pruned valid bound coordinates

V129 required both a valid class job and an `independent researcher-index job`, framed as whether a coordinate deserved its own stable card or selectable field. Across both materially different sources this suppressed many coordinates that perform real class-native roles inside represented relations but are not independently summary-worthy.

The correct boundary is not `standalone importance`. A primitive may be justified either by standalone source identity or by a real bound role in a material represented relation whose exact coordinate identity would otherwise disappear.

### 2. The natural-editorial-grain ceiling became semantic compression

V129 correctly rejected lexical census, but its ceiling test could treat local, nested, one-use, low-salience, or support coordinates as disposable texture even when the source actually established their class job. The durable successor must keep an anti-census ceiling while requiring an explicit enumerated suppression reason before deleting a source-supported class-native coordinate.

### 3. PLACE/TIME support became too independent-only

V129 admitted PLACE/TIME support only when independently researchable or strictly necessary to distinguish primary units. The paired failure shows that represented occurrence positions and episode/phase anchors can be legitimate researcher coordinates because they materially bind a relation even when they do not deserve standalone narrative treatment.

The successor restores relation-bound support while retaining the prohibition on clause/predicate microframes.

### 4. Relation classes were over-filtered by editorial independence

V129 paired relation necessity with a strong editorial-independence test. That prevented grammatical census but also removed source-supported LABEL, VERB, and LOCATOR coordinates whose local relation/orientation/state identity matters inside represented bindings.

The successor keeps complete lexical-grain and anti-grammar controls but admits local relation coordinates when they perform a real bound class job.

### 5. Primitive undercapture propagated into compound underconstruction

V129 compounds were limited to independently researchable primary multi-coordinate relations. Once valid primitives were suppressed, many local source-established bindings could not be reconstructed. The successor projects each material local binding once after primitive freeze, while continuing to prohibit pairwise closure, inferred edges, subset/superset duplicates, alternate tokenizations, and scene-wide mega-bundles.

### 6. The correction must not swing back to census

The V129 run also contained a smaller number of extra, over-broad, or unstable primitives. Therefore the successor is not an instruction to emit every mention, noun, predicate, preposition, or clause. It restores whole-source positive coverage and bound-coordinate entitlement while keeping bounded enumerated suppression and exact class-native grain.

## V130 correction

V130 prospectively:

1. replaces V129's standalone `own-card` threshold with dual entitlement: standalone source identity **or** a real bound class-native role in a material represented relation;
2. requires a positive whole-source coverage pass for each class before suppression;
3. requires an enumerated suppression reason rather than an unarticulated preference for semantic simplicity;
4. restores PLACE/TIME occurrence/episode supports when they materially anchor represented relations while continuing to prohibit predicate/clause microframes;
5. restores PERSON/OBJECT coordinates that participate in source-established relations even when minor, one-use, background, nested, or low-salience;
6. restores source-supported local LABEL/VERB/LOCATOR coordinates while retaining complete lexical-grain and anti-grammar controls;
7. preserves exact source lexical surface and job-based typing;
8. retains floor and ceiling audits, redefining the floor to include bound coordinates and the ceiling to target only enumerated census/duplication defects;
9. projects one compound for each source-established local multi-coordinate binding after primitive freeze, without graph closure or duplicate variants;
10. preserves all V126A harness/evaluator/isolation/history and one-shot holdout controls unchanged.

## What is retained unchanged

V130 retains:

- exactly seven primitive classes;
- immutable Case 2 / Case 6 workbook SHA verification before every execution;
- repair-from-authoritative-source requirement only when verification fails;
- exact source-span and lexical-preservation requirements;
- class-local identity/coreference and canonical-ID ownership by code;
- V64 compound-order correction;
- V66 field-aware evaluator correction;
- V88A editor-shorthand alignment correction;
- V92 same-session transient recovery behavior;
- V126A contract-subordinate request/compound harness correction;
- saved-agent fresh bootstrap from the declared durable contract;
- durable preservation of every attempt/failure;
- candidate-only / no-promotion / no-APA-ID boundaries;
- paired diagnostic plus repeated paired-archetype calibration gate;
- single calibrated-lineage Case 5 holdout gate;
- retirement of the calibrated lineage after a successful first holdout;
- brand-new saved-agent clean-room Case 5 certification gate.

## Isolation boundary

This correction record is administrative evidence and is not supplied to the worker. The V130 worker receives none of the Case 2 or Case 6 gold rows, expected counts, evaluator findings, scored predecessor outputs, canonical gold extracts, case-specific corrections/examples, sealed Case 5 material, or holdout output.

The worker receives only the complete supplied source, finalized V130 durable contract, contract-subordinate request envelope, response schema, and ordinary runtime metadata needed to execute.

## Holdout boundary

Case 5 remains sealed and unattempted in the V130 lineage. V130 must first pass one paired diagnostic and both required repeated paired verification batches under the same finalized contract and saved-agent lineage. Only then may that lineage access Case 5 exactly once. If it passes, that lineage is retired and a brand-new saved agent bootstrapped only from finalized V130 durable instructions must pass the sealed holdout in a fresh session for certification.

## Historical integrity

V129, V128, V127, V126A, every earlier contract/harness correction, every workflow run, candidate, finding, recovery artifact, and preserved failure remain intact. V130 controls only prospective calibration after the workflow is explicitly routed to it.
