# RESEARCHER INVENTORY V23 CALIBRATION CORRECTION RECORD

Status: ACTIVE BOUNDED SUCCESSOR — CALIBRATION ONLY
Date: 2026-09-14
Authority: Leah’s standing Researcher Inventory calibration directive
Predecessor contract: `researcher_inventory/AGENT_CONTRACT_V22.md`
Successor contract: `researcher_inventory/AGENT_CONTRACT_V23.md`
Latest predecessor evidence: workflow run `34804917505`

## 1. Classification

`IN-SCOPE BOUNDED CORRECTION / GAP CLOSURE`

This correction changes worker behavior only. It does not change immutable gold workbooks, evaluator answers, evaluator pass criteria, holdout isolation, candidate-only status, promotion authority, or APA identity rules.

## 2. Preconditions verified

The latest V22 workflow run verified both Leah-approved repository workbooks against the immutable SHA-256 values before calibration. No repair was required. Deterministic apparatus tests also passed.

The V22 calibration stage failed. The sealed holdout and clean-room stages were not reached.

## 3. Defect classification

### Agent-behavior defect — generalizable

V22 asked the worker to apply an additional global admission judgment after class membership: independent researcher-selectability/reconnective value and material semantic/navigation loss if omitted.

The failure evidence shows this extra significance gate is unstable for the intended inventory. It suppressed represented local coordinates, especially unnamed scene/time coordinates and small lexical relation/locator increments, while also producing inconsistent cross-class typing. The human-approved archetypal inventory is literal and class-local: coordinates are admitted because they are represented and actually perform the class job, not because the worker separately judges them globally important.

V22 also produced an explicit LABEL source span that was not a contiguous substring of the source. The deterministic exact-source validator correctly rejected it. This is a worker source-copy discipline defect, not a validator defect.

### Test-harness defect — not demonstrated in this run

Workbook verification passed. Deterministic apparatus tests passed. The current source-exactness rejection is correct. The admitted V20 evaluator-only normalization remains unchanged.

No evaluator/gold modification is authorized by this correction.

## 4. Corrective rule

V23 removes the extra importance/salience/reconnective/material-loss gate from worker-visible instructions.

Forward worker procedure is:

1. read the full source;
2. scan source order for every represented coordinate that actually performs the requested class’s positive job;
3. lock every explicit row to an exact contiguous source span before writing the tag;
4. resolve aliases/coreference;
5. remove only true same-class duplicates, unsupported invention, and grammatical debris;
6. preserve valid local, unnamed, nested, and cross-class-overlapping coordinates;
7. run a second independent omission/source-exactness verification pass;
8. build compounds only after units are final.

No case-specific archetype row, expected count, evaluator finding, or sealed-holdout content is placed in V23 worker instructions.

## 5. Historical-integrity effect

`AGENT_CONTRACT_V22.md` and all V22 code/history remain preserved.

`PARTIALLY SUPERSEDED — 2026-09-14`

For forward calibration behavior only, V23 supersedes V22’s global independent-coordinate/material-loss admission test and replaces it with represented-coordinate class membership plus exact-source locking.

Retained from V22:
- literal source preservation;
- class separation;
- alias/coreference handling;
- posture preservation;
- qualities boolean boundary;
- candidate-only status;
- no promotion / no APA IDs;
- archetype and holdout isolation;
- repeated archetype passes before holdout;
- fresh-agent clean-room certification requirement.

## 6. Holdout state

SEALED CASE 5: `NOT DEPLOYED`

It remains closed until the external harness demonstrates the required repeated Case 2 and Case 6 passes under one finalized V23 contract.

## 7. Acceptance test

V23 is not certified by being filed. It must pass:

1. immutable workbook hash verification;
2. deterministic apparatus tests;
3. one diagnostic Case 2/Case 6 batch;
4. two further repeated Case 2/Case 6 batches under the same finalized contract and saved-agent lineage;
5. sealed Case 5 exactly once on that calibrated lineage;
6. retirement of that lineage;
7. a brand-new saved agent created only from the finalized durable V23 instructions;
8. one fresh-session clean-room Case 5 pass with no prior-session or holdout-output access.

Only then may the apparatus be marked certified.
