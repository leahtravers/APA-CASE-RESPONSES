# Researcher Inventory V103 Calibration Correction Record

Status: `PROSPECTIVE SEMANTIC SUCCESSOR AUTHORIZED BY STANDING CALIBRATION SCOPE`
Date: 2026-09-19
Successor contract: `RI-CONTRACT-V103`
Predecessor: `RI-CONTRACT-V102`
Scope classification: `IN-SCOPE BOUNDED CORRECTION / GAP CLOSURE`
Executive Registrar record: `APA-EXEC-2026-09-19-CASES-RI-V103-CAL-0001`

## Preserved evidence

V102 is not edited or deleted. Its contract, task rules, runtime wrappers, pipeline, workflow execution, model/session evidence, evaluator findings, candidates, and failure history remain immutable calibration evidence.

Correction basis: workflow run `35477164717`, attempt 1.

That execution established:

- Case 2 SHA-256 matched `d47915552fdbdd23adc8c3f66ab06f94e022ab0bfd93faa5faabb53fdf9c9193`;
- Case 6 SHA-256 matched `50d646aad08b33051b93c1f3b3538d59ff78386558473b71a906a94213635070`;
- no archetype repair was required;
- all 21 deterministic apparatus/harness tests passed before semantic calibration;
- Case 2 and Case 6 both failed semantic archetype comparison during the first paired diagnostic batch;
- the repeatability batches did not run;
- the sealed holdout was not attempted;
- workflow evidence was preserved under `calibration_history/researcher_inventory/35477164717-A1/` and committed as `b8805a8f87975607626eafff46c109971f79e3b2`.

## Defect classification

`AGENT-BEHAVIOR / DURABLE SEMANTIC CONTRACT DEFECT — CANONICAL SPAN / CLASS ASSIGNMENT RESOLUTION`

The V102 failure demonstrates a generalizable requirement rather than a case-specific answer:

1. V102's deletion-counterfactual admission test is too strong as a universal gate. A valid lightweight coordinate can be low-salience, one-use, nested inside a larger relation, or locally descriptive without being indispensable to the whole semantic map.
2. Lightweight resolution must be achieved primarily through canonical span selection, semantic identity, deduplication, and class-native exclusions, not by requiring every primitive to carry independently indispensable global structure.
3. Primitive recovery must remain comprehensive across source-reified class-native facets before compression. Otherwise legitimate explicit and implicit supports, referents, characterizations, predicates, and orientations disappear.
4. Canonicalization must reject the opposite failure mode: lexical fragments, proposition wrappers, duplicate head-only variants, and wrong-class projections must not survive merely because wording is segmentable.
5. Compound defects remain downstream of primitive selection and canonicalization. The existing evaluator and deterministic harness remain healthy.

No new harness defect is demonstrated. The immutable workbook gate and all deterministic tests passed, so V103 leaves gold files and evaluator mechanics unchanged.

## V103 corrective rule

V103 adopts `COMPREHENSIVE SOURCE-REIFIED RECOVERY + CANONICAL CLASS-NATIVE PROJECTION`:

- first recover every distinct source-reified candidate facet in the requested class, including subtle, local, low-salience, one-use, remembered, hypothetical, prospective, figurative, and nested facets;
- admission does not require narrative centrality or proof that deletion would damage the whole semantic map;
- then canonicalize each candidate to the smallest complete source-native span that still performs the class-native job;
- classify by research function rather than part of speech or physical form;
- permit overlapping source traces across classes when they preserve genuinely different class-native facets;
- use class-specific exclusions, semantic identity, and deduplication to compress the inventory instead of a global materiality threshold;
- preserve legitimate explicit and neutral implicit PLACE/TIME support for distinct source frames without turning every clause or temporal/spatial token into a coordinate;
- retain complete predicate/posture units and orientation/context units when source-present, while rejecting grammar-only fragments;
- freeze canonical primitives before selective compound construction.

The correction is semantic and case-agnostic. Worker instructions contain no archetype rows, expected counts, evaluator findings, scored outputs, calibration answers, or sealed holdout content.

## Isolation and holdout boundary

Case 5 remains sealed. V103 must first pass one paired diagnostic batch and two paired repeatability batches for Case 2 + Case 6 under one finalized V103 contract and saved-agent lineage.

Only after those passes may the existing one-shot calibrated-lineage holdout gate execute. If that lineage passes Case 5, it is retired and a brand-new saved agent bootstrapped only from finalized V103 durable instructions must pass Case 5 in a new clean-room session without prior calibration-session or holdout-output access.

No candidate promotion, Oval Office admission, APA-ID minting, or APA Data Fabric/database write is authorized.

## Historical effect

`RI-CONTRACT-V102` remains authoritative for V102 historical runs. `RI-CONTRACT-V103` controls only new calibration/execution after the V103 routing commit.

Submission lane: `REGISTERED - RECEIPT CORRELATED` under Executive Registrar record `APA-EXEC-2026-09-19-CASES-RI-V103-CAL-0001`.
