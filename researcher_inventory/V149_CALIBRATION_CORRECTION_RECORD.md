# Researcher Inventory V149 Calibration Correction Record

Status: `PROSPECTIVE SEMANTIC SUCCESSOR — CALIBRATION ONLY`  
Date: 2026-09-21  
Successor contract: `researcher_inventory/AGENT_CONTRACT_V149.md` / `RI-CONTRACT-V149`  
Predecessor contract: `researcher_inventory/AGENT_CONTRACT_V148.md` / `RI-CONTRACT-V148`  
Authority: Leah's standing Researcher Inventory calibration instruction  
Executive Registrar record: `APA-EXEC-2026-09-21-CASES-RI-V149-CAL-0001`

## 1. Evidence gate

Latest completed V148 workflow: GitHub Actions run `35677345175`, conclusion `FAILURE`.

Before semantic execution, that run directly verified both Leah-approved repository workbooks byte-for-byte and required no repair:

- Case 2: `d47915552fdbdd23adc8c3f66ab06f94e022ab0bfd93faa5faabb53fdf9c9193`
- Case 6: `50d646aad08b33051b93c1f3b3538d59ff78386558473b71a906a94213635070`

All 34 deterministic apparatus/harness tests passed before model calibration. The attempt was preserved under `calibration_history/researcher_inventory/35677345175-A1/` by preservation commit `002632f`.

The sealed Case 5 holdout was not opened or deployed.

## 2. V148 failure classification

The completed evidence contains two different failure classes and they are kept separate.

### 2.1 Semantic worker / durable-contract defect

The Case 2 V148 candidate simultaneously:

- omitted distinct source-staged unnamed PLACE coordinates and several TIME phases/frames;
- omitted some source-treated OBJECT, LABEL, and LOCATOR coordinates;
- over-admitted dependent modifiers, support/copular fragments, discourse shells, partial predicate pieces, and underspecified orientation fragments;
- generated compound mismatch downstream from the wrong primitive inventory.

The pattern is not `too many` or `too few` in isolation. It is a grain-selection defect: V148 correctly made admission class-local, but its rule `genuinely performs the named class function` remained broad enough to admit semantic fragments that were not distinct inventory units while structural frame recall remained too conservative.

### 2.2 Lexical-integrity worker failure

The Case 6 V148 attempt failed the existing exact-source validator after retries because it proposed non-source wording (`with the sunrise`). The V148 contract already prohibited invented/noncontiguous wording. This is evidence that the worker did not satisfy an existing durable invariant, not evidence that the validator is wrong.

No case-specific correction is introduced from that phrase. V149 restates the exact-source rule in the complete-atom audit so a complete atom must still be an exact contiguous source span.

## 3. Cross-version reconciliation

V147 and V148 establish the two sides of the calibration boundary.

V147 used a global scaffold-level relation ceiling. Completed cross-archetype evidence showed that ceiling suppressed valid low-salience, local, unnamed, and relation-bound class coordinates.

V148 removed that global ceiling and made primitive selection class-local. The completed V148 Case 2 evidence shows that class-function membership alone can become too permissive: a dependent intensifier, support fragment, lexical sub-piece, or discourse shell may perform some local semantic function without being a distinct Researcher Inventory coordinate.

Therefore V149 does **not** restore V147's global ceiling. It introduces a second class-local question: whether the source stages the candidate as a distinct coordinate at that class's own inventory grain.

## 4. V149 corrective principle

V149 uses:

`whole-source frame map → positive class capture → class-local coordinate admission → complete-source-atom reconciliation → structural PLACE/TIME recall → PERSON/OBJECT recall and same-class identity reconciliation → frozen primitives → minimal compounds`

A primitive must satisfy both:

1. represented class function; and
2. source-staged inventory-coordinate identity at that class's own grain.

Neither test asks for global importance, recurrence, independent reuse, or a scaffold-level distinction.

## 5. Complete-source-atom correction

V148's `smallest exact contiguous source-native span` could be interpreted as the shortest lexical subspan that has class-like meaning. V149 changes this to the **smallest complete contiguous source-native atom**.

For LABEL, VERB, and LOCATOR:

- preserve the entire source unit needed to identify one characterization/relation/orientation move;
- retain polarity, degree, particle, modality, path, complement, or control wording when it belongs to the unit's identity;
- do not emit dependent intensifiers, bare support/copular/auxiliary/control fragments, bare prepositions/particles, or isolated deictics as standalone primitives when they only function inside a larger complete atom;
- split genuinely independent coordinated/serial moves, but do not split one control/predicate unit merely because several verbal words occur.

This is a general source-grain rule and contains no archetype answer.

## 6. Structural-coordinate correction

V149 strengthens structural recall without turning the inventory into an event census.

### PLACE

A named site is not required. A source-staged scene, interaction, participant position, remembered setting, later explanation, or present-telling setting can establish an unnamed PLACE when the where-context has distinct reconstructive identity, even if the exact physical location is unspecified or could coincide with another retained place.

### TIME

A date/time expression is not required. A source-staged action or interaction may anchor an unnamed TIME when the source presents it as a distinct phase/episode/frame through sequencing, transition, recurrence, intended duration, recollection, later explanation, or present reflection.

The limiting rule remains: not every predicate becomes a TIME. The event must function as a temporal frame, not merely happen sometime.

## 7. OBJECT correction

V149 retains V148's source-reification boundary and makes discourse-shell exclusion explicit.

A nominal or abstract expression is not a distinct OBJECT merely because it can be named. If its whole identity is just packaging of an already represented proposition/relation/binding, and the source does not separately treat it as a stable referent, exclude it.

One-use, abstract, choice-like, relational, or internal referents remain eligible when the source genuinely gives them object identity.

## 8. Compound rule

No independent compound-harness defect is demonstrated by run `35677345175`.

V149 therefore retains the deterministic compound evaluator and order-insensitive semantic membership correction. Compounds remain downstream of a frozen primitive inventory and may not repair primitive omissions or legitimize dependent lexical fragments.

## 9. Retained controls

V149 retains without weakening:

- exact source wording, contiguity, dialect, polarity, uncertainty, question, negation, correction, attribution, and posture preservation;
- same-class coreference/alias reconciliation;
- permission for low-salience, one-use, unnamed, deictic, remembered, reported, recurrent, intended, prospective, questioned, figurative, and present-telling coordinates when source-staged distinctly;
- source-reification boundary for OBJECT;
- type-by-function rule;
- deterministic apparatus ownership of IDs, numbering, validation, `_Q`, persistence, evaluator comparison, retry, and failure handling;
- immutable gold-workbook hash verification;
- worker isolation from gold/evaluator/holdout content;
- candidate-only storage and complete failure-history preservation;
- repeated Case 2/Case 6 pass gate;
- one-shot calibrated-lineage holdout and fresh-agent clean-room certification;
- prohibition on Oval Office promotion, APA-ID minting, and APA database/fabric mutation.

## 10. Historical-integrity effect

`RI-CONTRACT-V148` remains preserved as the contract that governed runs `35667494590`, `35677345175`, and other V148 attempts.

V148 is `PARTIALLY SUPERSEDED FOR FORWARD CALIBRATION — 2026-09-21` only for:

- treating represented class function as sufficient primitive admission without a distinct class-local inventory-coordinate test;
- the lexical interpretation of `smallest` where it permitted incomplete dependent sub-fragments rather than the smallest complete source-native atom;
- structural PLACE/TIME recall where distinct source-staged unnamed scenes/phases were underweighted;
- OBJECT admission where discourse shells or proposition packaging lacked independently source-treated referent identity.

V148's class-local orientation, exact-source lock, one-turn whole-source topology, gold isolation, deterministic apparatus controls, candidate boundaries, and holdout gates remain controlling unless separately superseded.

No predecessor is deleted or rewritten.

## 11. Worker-isolation boundary

The V149 worker receives no gold rows, expected counts, evaluator findings, canonical archetype extracts, scored predecessor outputs, case-specific hidden corrections/examples, sealed holdout material, or prior target answers.

This correction record may use evaluator and gold evidence to govern the calibration apparatus. That evidence is not copied into the worker contract as case-specific answers or examples.

## 12. Holdout state

The sealed Case 5 holdout remains closed. V149 must repeatedly pass both immutable Case 2 and Case 6 archetypes under the same finalized contract and saved-agent lineage before the one-shot holdout gate becomes eligible.

No V149 calibration correction may be made from Case 5 output.
