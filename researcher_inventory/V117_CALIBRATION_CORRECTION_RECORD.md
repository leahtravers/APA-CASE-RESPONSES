# Researcher Inventory V117 Calibration Correction Record

Status: `PROSPECTIVE SUCCESSOR CORRECTION`
Date: 2026-09-20
Authority: Leah's standing Researcher Inventory calibration instruction
Registrar: `APA-EXEC-2026-09-20-CASES-RI-V117-CAL-0001`
Predecessor: `RI-CONTRACT-V116`
Successor: `RI-CONTRACT-V117`

## Triggering evidence

V116 was exercised twice under the same finalized semantic contract and established hidden-evaluator/harness family in workflow run `35509533096`, attempts 1 and 2. Both attempts were preserved append-only under `calibration_history/researcher_inventory/35509533096-A1/` and `35509533096-A2/`.

Before semantic execution, the workflow verified the immutable Leah-approved Case 2 and Case 6 workbook SHA-256 values and required no repair. The deterministic apparatus/harness suite passed. Both paired semantic diagnostics failed before the sealed holdout was reached.

The second attempt was explicitly authorized as the one additional same-contract diagnostic needed to distinguish a one-off miss from a repeatable semantic defect. Its failure closes that diagnostic gate.

## Failure classification

`WORKER-SEMANTIC — GENERALIZABLE RECONNECTION-GRAIN / BINDING-COMPRESSION DEFECT`

This is not a workbook-integrity defect, deterministic harness defect, transport defect, evaluator-comparison defect, or holdout defect.

The repeated V116 pattern demonstrates both sides of the same problem:

- **over-atomization:** locally meaningful clause/content wrappers, predicate fragments, and orientation-like wording are admitted because each can be assigned a relation slot;
- **under-reconnection:** source-differentiated spatial/temporal supports and natural complete class-native coordinates can still be collapsed when they are not treated as independent micro-relations;
- **typing drift:** a source span can be assigned by lexical shape rather than by its actual reconnective job as characterization, operative relation, referent/content, or orientation;
- **predicate fragmentation:** a natural complete source action/choice can be split into smaller lexical predicates;
- **compound proliferation:** serializing every represented relation instance produces micro-compounds instead of the smaller set of materially complete source bindings.

V116's relation-slot closure is therefore too close to an exhaustive semantic-role inventory even though its intent was lightweight.

## Generalizable V117 requirement

V117 uses **SOURCE-RECONNECTION COORDINATE CLOSURE AT NATURAL FRAME GRAIN**.

1. Read the whole source and construct a small map of materially differentiated reconnection frames rather than an exhaustive relation-instance list.
2. Retain a primitive when it is source-grounded, class-native, naturally grained, class-locally distinct, and materially useful as a selectable reconnection coordinate.
3. Do not require recurrence, broad salience, narrative importance, or multiple uses. Local and one-use coordinates remain eligible.
4. Do not admit wording merely because it is a grammatical or semantic slot. If removing the row loses only wording already recoverable inside a stronger retained coordinate/binding, the row is excess.
5. PLACE/TIME support must close materially differentiated occurrence positions and episodes, including legitimate unnamed support, without creating one support row per clause.
6. Type OBJECT/LABEL/VERB/LOCATOR by represented reconnective function, not lexical shape.
7. Preserve natural complete predicate kernels; do not split coordinated/bound predicates when they jointly express one source-native action, choice, movement, or relation kernel.
8. Compounds serialize materially complete source bindings. They do not exist merely because every relation instance needs a compound, and a valid primitive may remain unbundled.

## Retained controls

V117 retains without weakening:

- immutable Case 2/Case 6 SHA-256 verification before semantic use;
- exact-source lexical and source-posture preservation;
- seven-class functional typing;
- one-use/local/low-salience eligibility;
- legitimate unnamed PLACE/TIME support;
- true coreference and class-local deduplication;
- primitive freeze before compounds;
- current deterministic evaluator and V66/V88A harness corrections;
- saved-agent and evaluator separation;
- sealed-holdout one-shot protection;
- clean-room fresh-agent certification;
- append-only attempt/failure history;
- candidate-only status;
- no promotion, no Oval Office research admission, no APA-ID minting, and no APA database mutation.

## Superseded prospectively

V116's rule that each materially represented relation instance should be closed across its class-native slots and then serialized as a distinct compound is superseded for forward calibration to the extent that it creates exhaustive micro-relation coverage rather than a lightweight source-reconnection index.

V116 remains complete historical evidence and controls work actually executed under it.

## Harness disposition

No deterministic harness or evaluator change is authorized by the V116 failure. The workbook-integrity gate and deterministic harness tests passed before both semantic diagnostics. V117 changes worker semantic authority and neutral task rules only.

The established evaluator-only corrections remain in force, including field-aware matching, compound-order normalization, and bounded editor-shorthand alignment. A later harness change requires separate evidence of a mechanical evaluator defect.

## Holdout disposition

`HELD — ARCHETYPE GATES NOT SATISFIED`

Sealed Case 5 remains inaccessible until V117 passes one paired diagnostic batch and two paired repeatability batches for Case 2 + Case 6 under the same finalized V117 contract and saved-agent lineage.
