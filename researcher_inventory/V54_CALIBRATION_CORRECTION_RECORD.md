# V54 CALIBRATION CORRECTION RECORD

Status: ACTIVE ADDITIVE SUCCESSOR RECORD
Date: 2026-09-16
Successor contract: `RI-CONTRACT-V54`
Predecessor contract: `RI-CONTRACT-V53`
Preserved predecessor run: `35087652789`

## Finding

The latest valid V53 run passed immutable archetype-integrity verification and executed through the version-correct V53 contract-isolated adapter. Its failure therefore is not attributable to the earlier contract-version harness mismatch.

The preserved V53 calibration output demonstrates a broad under-inventory pattern across both approved archetypes: the relation-integrity correction was applied too aggressively, causing source-distinguished independent coordinates to be suppressed when they were nested, unnamed, grammatically dependent, temporally co-present, or participating in a larger relation.

This is a generalizable agent-behavior defect because it recurs across both calibration archetypes and multiple inventory classes. It is not a case-specific gold correction.

## V54 correction

V54 retains V53's protection against syntactic census and over-fragmentation while adding an exhaustive-decomposition requirement:

1. build a full source-wide candidate ledger before collapse;
2. judge independent class-native research function after context is restored;
3. do not use grammatical dependence, nesting, lack of a proper name, episode co-membership, or participation in another relation as automatic exclusion;
4. split genuinely distinct lexical relation jobs even when embedded/infinitival/coordinated/reported, while preserving one relation when multiple tokens jointly form one semantic identity;
5. permit supported unnamed TIME/PLACE and relation-participating OBJECT/LABEL/LOCATOR units when independently reusable;
6. freeze primitive units before compound construction so compounds recombine proven source bindings rather than replacing primitive inventory.

## Isolation

No approved archetype rows, expected counts, evaluator answers, case-specific gold corrections, sealed holdout source, or holdout outputs are included in V54 instructions. The gold workbooks remain evaluator-side only.

## Historical integrity

V53 remains unchanged and authoritative historical evidence for work executed under it. V54 prospectively controls new calibration/certification work only. Existing calibration history is preserved; no failed attempt is deleted or relabeled.

## Holdout gate

Sealed Case 5 remains closed. V54 must first pass the immutable Case 2 and Case 6 verification gate repeatedly under one finalized saved-agent lineage. Only then may the existing one-shot sealed holdout workflow proceed. Certification still requires a second holdout success by a brand-new saved agent in a clean session with only finalized V54 durable instructions.
