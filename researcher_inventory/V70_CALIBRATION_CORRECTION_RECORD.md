# V70 Researcher Inventory Calibration Correction Record

Status: `ACTIVE FOR FORWARD CALIBRATION`  
Date: 2026-09-17  
Authority: Leah's standing Researcher Inventory calibration instruction  
Predecessor: `RI-CONTRACT-V69`  
Triggering preserved run: `35225459016`  
Executive filing: `APA-EXEC-2026-09-17-CASES-RI-V70-CAL-0001`

## Contract administration packet

- **Classification:** `IN-SCOPE BOUNDED CORRECTION / GAP CLOSURE`.
- **Parent authority:** Leah's standing Researcher Inventory calibration instruction, which authorizes durable-contract revision when a preserved failure demonstrates a generalizable requirement.
- **Mission:** correct V69's demonstrated cross-fixture semantic over-admission/type-resolution defect without reopening the V66 evaluator correction or weakening immutable-archetype, holdout-isolation, literal-source, candidate-only, or no-promotion controls.
- **Object set:** `AGENT_CONTRACT_V70.md`, `v70_task_rules.py`, V70 adapter/calibration/holdout entry points, the active Researcher Inventory verification workflow's version bindings, and this correction record.
- **Explicit exclusions:** no sealed Case 5 exposure during calibration; no holdout reuse; no gold rows/counts/evaluator findings in worker instructions; no database/schema mutation; no Oval Office research admission; no APA fabric promotion; no APA-ID minting; no deletion or silent rewrite of V69 or earlier evidence.
- **Owning branch/department:** APA Cases owns Researcher Inventory candidate/calibration semantics and training history. GitHub Actions supplies execution transport only.
- **Crew/integrator:** Presidential Executive Assistant under Leah's standing calibration order; no cross-department master-contract integration is required for this bounded successor.
- **Dependencies:** immutable workbook verification; deterministic apparatus tests; V66 field-aware evaluator correction; existing saved-agent/session bootstrap and recovery implementation; current Case 2/Case 6 hidden evaluators; Executive Registrar background filing.
- **Required returns:** immutable-hash result; deterministic harness result; hidden archetype calibration result; durable training-history preservation; registrar receipt when returned. No independent Audit certification is claimed.
- **Acceptance criteria:** Case 2 and Case 6 both pass the diagnostic batch and two repeated two-run batches under exactly `RI-CONTRACT-V70` on one bounded saved-agent lineage. Only then may the existing one-shot sealed holdout gate open.
- **Evidence:** workflow run IDs, commit SHA, run manifest, `archetype_integrity.json`, complete `training_history.ndjson`, `test_results.json`, session-recovery traces, exact contract snapshot, holdout-stage state, and registrar filing record.
- **Failure semantics:** a semantic mismatch after passing hash/harness gates is `AGENT-BEHAVIOR / DURABLE-INSTRUCTION DEFECT`; deterministic apparatus failure is `HARNESS DEFECT`; upstream failure leaves holdout `NOT_DEPLOYED`; uncertain transport is reconciled rather than blindly rerun.
- **No-deletion rule:** preserve every predecessor contract, failed candidate, run, evaluator finding, recovery trace, and correction record. No destructive cleanup is authorized.
- **Execution boundary:** this successor authorizes new calibration only. It does not itself authorize holdout execution; the workflow's repeated-archetype gate controls that transition.
- **Completion/return:** certification requires the later one-shot calibrated-lineage holdout success plus fresh-agent clean-room success under finalized V70 instructions. Otherwise calibration remains active and Leah is not notified unless a hard blocker requires her action.

## Immutable archetype and harness gates

Run `35225459016` passed the immutable Case 2 / Case 6 workbook verification step with the required SHA-256 values and required no repair. It also passed deterministic apparatus verification before semantic calibration failed. The sealed Case 5 holdout was not reached or consumed.

## Defect separation

Classification: `AGENT-BEHAVIOR / DURABLE-INSTRUCTION DEFECT — GENERALIZABLE`.

The V69 semantic attempt over-corrected V68's earlier under-resolution. Across both calibration fixtures it admitted large numbers of clause-local TIME, proposition/content OBJECT, characterization/predicate LABEL, subpredicate VERB, and contextual LOCATOR units that did not have independent archetypal coordinate identity. Broad overlap permission also encouraged duplicate projections and compound inflation. At the same time it still missed supported unnamed scene/frame anchors and some source-native type distinctions. One candidate also introduced normalized wording instead of the exact source lexeme.

Because immutable workbook verification and deterministic apparatus tests passed first, these findings are not attributed to the current V66 harness/evaluator implementation.

## V70 generalized correction

V70 establishes a middle grain between V68 over-compression and V69 over-admission:

- single-use coordinates remain admissible, but every primitive must be independently selectable inside the represented source map;
- PLACE/TIME use a scene/frame ledger and explicitly preserve supported unnamed object/participant locations, destinations, waiting scenes, later interactions, present telling/reflection, recurring spans, and prospective frames without creating one coordinate per predicate or action phase;
- OBJECT preserves stable concrete/abstract things, decisions/options/relations/situations while rejecting whole-proposition, question, reason-clause, and event-restatement objects;
- LABEL preserves the shortest complete characterization value rather than whole proposition/predicate packets;
- VERB returns to lexical relation increments, while expressly retaining cognition/stance/report/recall/intention and other matrix relations when they materially contribute epistemic, attributional, modal, or relational posture;
- LOCATOR requires an independently selectable orientation job and does not duplicate a motion/state predicate merely because its wording is directional-looking;
- cross-class projection is conservative and requires separately addressable class jobs;
- literal-source preservation is reaffirmed for wording and source-derived tags;
- compounds are constructed only after primitives are frozen and cannot be used to justify extra primitives.

These rules are fixture-neutral. No approved archetype rows, expected counts, evaluator answers, prior scored outputs, or sealed holdout content are placed in the worker contract or task rules.

## Historical integrity

V69 and all predecessors remain unchanged and authoritative for their historical runs. The active workflow predecessor is preserved separately in `researcher_inventory/workflow_history/researcher-inventory-contract-test-v69-2026-09-17.yml` before the live workflow pointer is moved to V70.

V70 supersedes V69 only for new calibration and any later holdout lawfully reached after repeated Case 2 + Case 6 passes under the exact same finalized V70 contract.
