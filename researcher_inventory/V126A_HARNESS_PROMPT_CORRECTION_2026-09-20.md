# Researcher Inventory V126A Harness Prompt Correction

Status: `ACTIVE HARNESS SUCCESSOR — DURABLE CONTRACT UNCHANGED`  
Date: 2026-09-20  
Durable semantic contract: `RI-CONTRACT-V126`  
Predecessor harness: V126 runtime/task-prompt layer  
Registrar: `APA-EXEC-2026-09-20-CASES-RI-V126A-HARNESS-0001`  
Authority: Leah's standing Researcher Inventory calibration instruction

## Why this is a harness correction

Workflow run `35542859594` verified both Leah-approved archetype workbooks against their required SHA-256 values and passed all 21 deterministic apparatus/harness tests before the paired Case 2 / Case 6 diagnostic. The diagnostic then failed and the complete attempt was preserved at `calibration_history/researcher_inventory/35542859594-A1`.

Review of the worker-facing runtime instruction stack independently established a prompt-composition defect that exists before hidden-archetype comparison:

- `AGENT_CONTRACT_V126.md` requires frame-first reconstruction, admission only for a distinct source-local binding role, and expressly states that represented meaning, mention, or grammatical independence alone is insufficient.
- `v126_task_rules.py` nevertheless constructed its request rules by prepending the legacy `inventory_apparatus.BASE_RULES` and then appending the V126 correction.
- the inherited legacy rules instructed the worker to act as a `comprehensive literal extraction worker`, said that `Do less` did not mean fewer materially represented coordinates, and used broad class directives such as returning every independently selectable characterization and materially represented lexical predicate increments;
- the inherited compound layer also added its own broader compound-selection semantics on top of the durable contract.

Those request-level directives materially conflict with the finalized V126 admission rule. The resulting run therefore is not clean evidence that V126's durable semantic rule itself needs another revision.

## Correction

V126A changes only the runtime/harness prompt layer.

1. `AGENT_CONTRACT_V126.md` remains the sole durable semantic authority supplied when the saved Researcher Inventory agent is created.
2. Per-request `rules` become a narrow execution envelope: they select the requested operation, require JSON/schema/source fidelity and isolation, and explicitly may not broaden or reinterpret the durable contract.
3. Per-class `class_rule` values become selectors that direct the worker to apply the corresponding class rule already present in the durable V126 contract. They add no independent admission criteria.
4. Compound construction uses a V126A harness subclass whose request says to apply the durable contract's compound rule exactly and use only frozen supplied unit refs. It does not reintroduce the predecessor's event/proposition census or compound-expansion language.
5. Deterministic regression tests verify that the V126A request layer contains the contract-authority boundary and does not contain the identified conflicting legacy phrases.

## What does not change

V126A does not change:

- the seven primitive classes;
- `AGENT_CONTRACT_V126.md`;
- approved Case 2 / Case 6 workbook bytes or expected hashes;
- hidden evaluator answers;
- V66 field-aware evaluator correction;
- V88A editor-shorthand alignment correction;
- source-span, canonical-ID, ordering, Q, reference, or SQL-shape mechanics;
- durable attempt/failure preservation;
- same-session transient recovery;
- the repeated paired-archetype gate;
- the one-shot sealed Case 5 gate;
- the clean-room new-saved-agent gate;
- candidate-only / no-promotion / no-APA-ID boundaries.

## Evidence classification rule after V126A

A V126A run that passes immutable workbook verification and deterministic harness tests but still fails hidden Case 2 / Case 6 comparison may be evaluated as worker-semantic evidence because the independently demonstrated prompt conflict has been removed.

A later durable contract successor is permitted only if that clean failure demonstrates a generalizable semantic requirement. Do not infer such a requirement from the contaminated V126 run `35542859594`.

## Holdout boundary

The sealed Case 5 source remains inaccessible to calibration. V126A may reach the first and only calibrated-lineage holdout only after one paired diagnostic and both required repeated paired verification batches pass under the same finalized V126 contract and saved-agent lineage. If that holdout passes, the calibration agent is retired and a brand-new saved agent containing only finalized V126 durable instructions must pass Case 5 in a fresh session for certification.

No holdout source or output is reproduced in this record.

## Historical integrity

V126 and every predecessor contract, task-rule file, runner, workflow run, candidate, finding, and recovery artifact remain preserved. V126A is additive and controls only prospective runtime/harness prompt composition after its routing commit.
