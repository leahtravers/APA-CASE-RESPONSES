# Researcher Inventory V87 → V88 Successor Pointer

Effective date: 2026-09-18  
Authority: Leah's standing Researcher Inventory calibration instruction  
Predecessor: `researcher_inventory/AGENT_CONTRACT_V87.md` (`RI-CONTRACT-V87`)  
Successor: `researcher_inventory/AGENT_CONTRACT_V88.md` (`RI-CONTRACT-V88`)  
Evidence: `calibration_history/researcher_inventory/35410859798-A1/`  
Executive Registrar: `APA-EXEC-2026-09-18-CASES-RI-V88-CAL-0001`

## PARTIALLY SUPERSEDED — FOR NEW CALIBRATION ONLY

V87 remains immutable historical authority for every execution that used it. V88 prospectively supersedes only V87's worker-semantic admission/grain and compound-construction rules for new calibration and any later holdout lawfully reached under V88.

## Preserved evidence and failure basis

Run `35410859798-A1` verified both immutable approved workbook copies against Leah's exact SHA-256 values with no repair, passed deterministic apparatus/harness checks, and then failed both approved archetype comparisons under V87. The same class-level mechanism appears in both cases: V87 compressed independently useful lexical predicate increments, orientations, occurrence positions, episode frames, and separately staged values into broader handles, while some broad reifications remained non-archetypal.

The repeated cross-case failure is therefore classified as an agent/contract semantic defect, not a workbook-integrity or demonstrated deterministic-harness defect.

## Generalizable correction

V88 establishes class-local independent-coordinate grain:

- evaluate each requested class independently;
- retain the smallest sufficient coordinate when removing it from that class would erase a materially distinct reusable function;
- allow the same source span to support multiple classes when it independently performs different jobs;
- preserve lexical predicate increments rather than clause-sized relation bundles when the predicates do different work;
- preserve independently useful locator/orientation relations even when wording overlaps motion, place, time, or compounds;
- preserve materially distinct PLACE/TIME occurrence positions and frames even when they overlap broader settings/periods;
- preserve source-staged LABELs independently from qualities and neighboring classes;
- reject unsupported OBJECT proposition-shell nominalization; and
- restore proposition/event-level compound coverage over frozen primitives.

## Retained unchanged

- exact immutable Case 2 and Case 6 SHA-256 gates and authoritative reconstruction on mismatch;
- V66 field-aware evaluator/harness correction;
- V82 workflow-attempt history durability;
- worker/evaluator isolation;
- literal source-language preservation;
- transport/session recovery;
- candidate-only status and no-promotion rule;
- preservation of every failed attempt and training lineage;
- repeated-archetype-pass requirement under one finalized contract;
- one-shot sealed Case 5 holdout law;
- retirement of the calibrated lineage before clean-room verification;
- fresh saved-agent certification with no prior session or holdout-output access;
- no Oval Office promotion, APA ID minting, sovereign/admitted writing, or APA database writes.

No sealed Case 5 content or output was read or used to derive V88.
