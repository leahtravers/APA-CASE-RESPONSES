# V112 Researcher Inventory Calibration Correction Record

Status: `PROSPECTIVE SEMANTIC SUCCESSOR FILED`
Date: 2026-09-20
Predecessor contract: `RI-CONTRACT-V111`
Successor contract: `RI-CONTRACT-V112`
Evidence run: `35495173388`, attempt 1
Registrar record: `APA-EXEC-2026-09-20-CASES-RI-V112-CAL-0001`

## Immutable archetype gate

Before semantic calibration, run `35495173388` directly verified the repository copies of Leah-approved workbooks against the required SHA-256 values:

- `Case_2_Lightweight_Researcher_Inventory.xlsx` = `d47915552fdbdd23adc8c3f66ab06f94e022ab0bfd93faa5faabb53fdf9c9193`
- `Case_6_Lightweight_Researcher_Inventory_v2_with_Verbs_and_Locators.xlsx` = `50d646aad08b33051b93c1f3b3538d59ff78386558473b71a906a94213635070`

Result: `VERIFIED`; no repair was required.

## Harness classification

All 21 deterministic apparatus/harness tests passed before model calibration. The paired Case 2/Case 6 hidden semantic comparisons then failed. The workflow preserved the complete failed attempt append-only under `calibration_history/researcher_inventory/35495173388-A1/`, including archetype-integrity evidence, run manifest, iteration contract, test results, training history, holdout-stage state, and bounded same-session recovery evidence.

Finding: `WORKER-SEMANTIC CLASS-LOCAL COVERAGE / FUNCTIONAL-TYPE DEFECT`, not workbook-integrity or deterministic-harness defect.

The sealed Case 5 holdout was not reached or opened.

## Generalizable failure pattern

V111 replaced V110's represented-role gate with a global independent-research-coordinate test. The latest paired evidence shows that this still asks the worker to perform economy at the wrong level. Across both approved archetype cases the worker simultaneously:

- omitted legitimate class-native scene/phase support, simple relation predicates, source-assigned characterizations, stable handles, and orientations when neighboring units or compounds appeared to carry related meaning;
- emitted some larger clause-like or discourse/content wrappers instead of the smallest class-native span;
- allowed surface syntax to pull characterization/state material into OBJECT or LOCATOR rather than assigning by represented function; and
- treated some purpose/topic/temporal fragments as LOCATOR even though they did not perform an orienting relation.

The pattern is cross-case and cross-class. It is not a missing evaluator rule, workbook defect, hidden-count problem, or case-specific lexical issue.

The durable distinction is that Researcher Inventory is seven **orthogonal class ledgers**. Admission and deduplication must be decided inside the requested class. A valid unit in one class must not be suppressed because another class or a later compound expresses related source meaning.

## V112 correction

`RI-CONTRACT-V112` therefore prospectively replaces V111's inventory-global coordinate economy with:

1. **class-local selectability** — a source-established candidate is retained when it is a distinct selectable entry of the requested class at natural literal grain;
2. **orthogonal class coverage** — each class is inventoried independently over the whole source, so cross-class overlap does not itself create redundancy;
3. **same-class economy only** — aliases, coreference, true restatements, and same-class duplicates merge, but a different class is not a substitute;
4. **functional precedence** — LABEL wins when a span's represented function is characterization/state/evaluation even if its syntax looks nominal, locative, or comparative; LOCATOR requires a genuine orienting relation; OBJECT requires thing/content identity; VERB uses the smallest lexical predicate construction; and
5. **coverage-matrix freeze** — each differentiated source scene/episode is revisited across all seven classes to detect low-salience omissions before anti-census, functional-type, atomic-span, and literal-lock audits.

PLACE and TIME remain differentiated support ledgers and allow legitimate unnamed support. VERB remains lexical-relation inventory rather than clause inventory. LOCATOR excludes ordinary purpose/beneficiary/topic/degree/aspect wording without a true orientation function. Compounds are built only after all class ledgers freeze and may not substitute for omitted primitives.

No archetype rows, hidden counts, evaluator findings, scored outputs, calibration answers, or holdout material are included in the V112 worker contract or task rules.

## Holdout state

Case 5 remains inaccessible until one paired diagnostic batch and two paired repeatability batches for Case 2 + Case 6 pass under the same finalized V112 contract and saved-agent lineage. A calibrated-lineage holdout attempt remains one-shot. If that lineage passes, it must be retired before a brand-new saved agent bootstrapped only from finalized V112 durable instructions and isolated from all calibration sessions and holdout output performs clean-room verification.

## Historical integrity

V111 remains intact and controls its historical runs. V112 applies prospectively only after workflow routing changes to V112. No predecessor contract, failed run, evaluator record, calibration-history directory, immutable workbook, or saved attempt is deleted or rewritten.

## Candidate-only boundary

All calibration outputs remain candidate research. No Oval Office promotion/admission, APA-ID minting, sovereign/admitted write, or APA Data Fabric/database mutation is authorized by this correction.
