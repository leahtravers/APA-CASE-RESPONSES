# RESEARCHER INVENTORY V18 CALIBRATION CORRECTION RECORD

Status: CALIBRATION HISTORY / FORWARD CORRECTION RECORD
Date: 2026-09-13
Authority: Leah's standing Researcher Inventory calibration directive
Predecessor calibration contract: `AGENT_CONTRACT_V17.md`
Successor calibration contract: `AGENT_CONTRACT_V18.md`
Predecessor failed workflow run: `34778712289`

## Immutable gold gate

Before this correction, the repository's two Leah-approved archetype workbooks were verified by the calibration harness against the exact required SHA-256 values. The preserved integrity record for workflow run `34778712289` reports `VERIFIED` before and after the gate and no repair.

No archetype workbook, canonical evaluator extract, evaluator criterion, expected count, or holdout object is changed by V18.

## Preserved predecessor evidence

The complete V17 failed attempt remains under:

`calibration_history/researcher_inventory/34778712289/`

It remains failed calibration evidence and has no certification effect.

## Defect class 1 — worker-behavior / instruction defect

V17 correctly repaired the earlier deletion bias, but its replacement completeness language became too broad. It repeatedly treated source grounding and describability as sufficient reasons to create rows.

The result was systematic over-resolution across requested classes: sub-actions became separate scene/time coordinates without distinct class jobs; grammatical or descriptive material became independently selectable objects/labels/verbs/locators; and the resulting excess coordinates destabilized ordering and compound construction.

This is generalizable. It is not a case-specific exception.

V18 therefore restores the class-local independent-coordinate gate:

> retain a row only when removing it from that class would remove a materially distinct researcher-selectable function of that class.

The gate is symmetric: it forbids both deletion of a materially distinct coordinate and retention of a merely source-grounded redundant/grammatical/qualifying coordinate.

## Defect class 2 — lexical-copy interface robustness

The V17 oil-change calibration terminated before semantic evaluation because the worker repeatedly returned `source_wording` that was not an exact contiguous substring of the source.

The exact-source validator is correct and is retained unchanged. Lexical preservation must not be repaired with fuzzy matching, synonym substitution, or silent normalization.

The V18 worker contract and adapter instead make the interface requirement explicit: copy `source_wording`, `source_cue`, and `order_cue` character-for-character from the source, including punctuation/apostrophes/hyphens/casing, and use the shortest complete lexical construction rather than rebuilding a clause from memory.

This is a harness/task-interface clarification, not a relaxation of lexical verification.

## Neutral apparatus correction

The V17 adapter correctly passed neutral apparatus rules through to the worker, but those neutral rules inherited the same over-resolution pressure as V17.

V18 uses a versioned calibration wrapper that supplies balanced class rules consistent with the durable V18 independent-coordinate gate. The historical core apparatus and V17 wrapper remain preserved.

## Evaluation boundary

The evaluator remains hidden from the worker and continues to run only after candidate production. Cross-class diagnostics and canonical extracts do not enter worker input.

V18 does not encode archetype rows, case-specific phrases, expected counts, or evaluator findings into the durable contract, adapter, or neutral task rules.

## Holdout boundary

No sealed Case 5 content or output was accessed, used, or incorporated in this correction.

Case 5 remains unavailable until repeated Case 2 and Case 6 passes occur under the same finalized V18 contract and saved-agent lineage. Only then may the calibrated lineage run the sealed holdout exactly once. A successful first holdout must be followed by retirement of that lineage and a brand-new saved agent bootstrapped only from the finalized durable instructions for clean-room verification.

## Candidate-only boundary

Calibration outputs remain candidate/test material. V18 grants no promotion authority, creates no APA identity, and authorizes no Oval Office research write.

## Forward state

V18 is authorized for calibration only. Certification requires the full repeated-archetype, sealed-holdout, and fresh-agent clean-room gates already established by the standing workflow.
