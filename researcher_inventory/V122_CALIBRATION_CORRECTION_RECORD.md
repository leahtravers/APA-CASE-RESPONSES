# V122 RESEARCHER INVENTORY CALIBRATION CORRECTION RECORD

Status: `ACTIVE SUCCESSOR FOR CALIBRATION — NOT CERTIFIED`
Effective date: 2026-09-20
Authority: Leah's standing Researcher Inventory calibration instruction
Predecessor: `RI-CONTRACT-V121`
Successor: `RI-CONTRACT-V122`
Registrar: `APA-EXEC-2026-09-20-CASES-RI-V122-CAL-0001`

## Triggering evidence

V121 workflow run `35530289086` attempt 1 executed only after the repository copies of Leah-approved Case 2 and Case 6 workbooks were verified exactly against the immutable SHA-256 values:

- `Case_2_Lightweight_Researcher_Inventory.xlsx` = `d47915552fdbdd23adc8c3f66ab06f94e022ab0bfd93faa5faabb53fdf9c9193`
- `Case_6_Lightweight_Researcher_Inventory_v2_with_Verbs_and_Locators.xlsx` = `50d646aad08b33051b93c1f3b3538d59ff78386558473b71a906a94213635070`

Repair required: none.

All 21 deterministic apparatus/harness tests passed before semantic execution. The first paired diagnostic batch then failed semantically on both archetypes. The attempt and failure evidence were preserved under `calibration_history/researcher_inventory/35530289086-A1/` by history commit `5280c67`.

Case 5 was not reached and remains `NOT_DEPLOYED`.

## Defect classification

`WORKER-SEMANTIC DEFECT — EDITORIAL RESOLUTION / MATERIAL-BINDING MISALIGNMENT`

No new workbook-integrity, deterministic-harness, evaluator, transport, or holdout defect is demonstrated.

V121 correctly established class-relative individuation and correctly rejected a pure surface-span census. Its remaining failure is more fundamental: it still asks the worker to decide primitive existence primarily one class at a time. Across both archetypes, that produces the same two-sided drift:

1. materially differentiated supports and source-native relation/characterization/orientation units can be omitted because they look local, colloquial, structurally small, or insufficiently entity-like; and
2. literal spans can still be over-admitted because they look semantically plausible in isolation even though they do not occupy an independently useful role in the represented case structure.

The archetypal inventory therefore requires an editorial resolution test grounded in the represented case relations before class-by-class emission.

## Generalizable V122 requirement

V122 establishes:

`MATERIAL-BINDING-CAPABLE RESEARCHER COORDINATE AT SOURCE-NATIVE EDITORIAL GRAIN`

Before emitting any primitive class, the worker must privately map the source into materially distinct represented bindings and their support frames. A candidate primitive is admitted when it is a source-native coordinate that can occupy one independently useful role in that represented map, even if the coordinate is local, one-use, colloquial, uncertain, negated, figurative, or remains unbundled in the final compound set.

The binding map is an **editorial resolution scaffold**, not a requirement that every primitive appear in a final compound. It prevents both lexical census and sparse summarization.

### Class consequences

- `PLACE` and `TIME`: derive from materially differentiated support frames needed to situate represented bindings, including legitimate unnamed frames and present-telling/reflection frames when the source itself shifts into them.
- `PERSON` and `OBJECT`: admit represented participants/endpoints/content handles that function as independently reusable coordinates in the binding map; reject mere grammatical complements, discourse shells, and casually mentionable nouns.
- `LABEL`, `VERB`, and `LOCATOR`: admit source-native characterization/relation/orientation kernels that perform one material role in a represented binding or support relation; one-use units remain eligible. Reject literal spans that contribute only wording, style, grammar, scene color, or redundant restatement.

### Binding-first, primitive-freeze, compound-second sequence

1. Read the entire source.
2. Build a private material-binding/support map without emitting output.
3. Derive candidate primitives from roles in that map plus legitimate source-native standalone coordinates.
4. Run class-local coverage/compression/type arbitration.
5. Freeze primitives.
6. Build only the selective final compounds required by materially distinct bindings.

This is not permission to provide the worker archetype rows, expected counts, evaluator findings, or case-specific examples.

## Retained controls

V122 retains without weakening:

- immutable Case 2/Case 6 SHA-256 verification before semantic use;
- all seven primitive classes;
- exact lexical/source-posture preservation;
- legitimate unnamed PLACE/TIME support;
- legitimate local and one-use coordinates;
- true coreference and class-local deduplication;
- natural minimal literal grain;
- primitive freeze before final compounds;
- selective compounds rather than graph closure;
- V66 field-aware evaluator correction;
- V88A editor-shorthand alignment correction;
- durable attempt/failure history;
- same-session transient recovery;
- saved-agent/evaluator separation;
- sealed-holdout one-shot protection;
- clean-room fresh-agent certification;
- candidate-only status;
- no promotion, no Oval Office research admission, no APA-ID minting, and no APA database mutation.

## Harness disposition

No deterministic harness or evaluator semantic change is authorized by the V121 failure. V122 may add versioned worker-facing task rules and wrappers that route the existing proven evaluator/isolation mechanics unchanged.

## Historical integrity

`RI-CONTRACT-V121` and all earlier contracts, correction records, runtime files, attempts, outputs, and findings remain preserved.

`RI-CONTRACT-V122` controls only new calibration executions after activation. It does not retroactively alter or relabel prior results.

## Holdout boundary

`HELD — ARCHETYPE GATES NOT SATISFIED`

Case 5 remains inaccessible until V122 passes the paired diagnostic and required repeated paired Case 2 + Case 6 batches under the exact same finalized V122 contract and saved-agent lineage.

The sealed holdout must not be exposed to calibration, instructions, examples, or contract revision.
