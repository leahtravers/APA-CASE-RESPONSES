# V43 Researcher Inventory Calibration Successor Record

Status: `CALIBRATION SUCCESSOR — CANDIDATE ENVIRONMENT ONLY`
Date: 2026-09-15
Predecessor: `RI-CONTRACT-V42`
Successor: `RI-CONTRACT-V43`
Evidence run: `34996430918`
Evidence head: `3b58da44e07dab4629ac689ad4ceaa8da50228b4`
Registrar record: `APA-EXEC-2026-09-15-CASES-RI-V43-CAL-0001`

## Archetype integrity gate

The evidence run verified both immutable Leah-approved workbooks before calibration and recorded no repair:

- Case 2 SHA-256: `d47915552fdbdd23adc8c3f66ab06f94e022ab0bfd93faa5faabb53fdf9c9193`
- Case 6 SHA-256: `50d646aad08b33051b93c1f3b3538d59ff78386558473b71a906a94213635070`
- Integrity status: `VERIFIED`
- Repaired: none

The workbook gate therefore did not cause the V42 failure.

## Harness-versus-worker adjudication

The V42 workflow injected the exact `AGENT_CONTRACT_V42.md` contract into a newly bootstrapped saved agent, used the V42 isolated adapter, and passed deterministic apparatus/source validation. Both cases then failed hidden semantic archetype adjudication.

The shared pattern is semantic-grain behavior rather than harness failure: broad bindings often replaced smaller independently research-bearing components, while some discourse/narrative wrappers were still retained. Missing-unit and type errors then cascaded into compound mismatch. The apparatus itself is unchanged except for prospective version pointers.

## Prospective correction

V43 preserves V42's frame skeleton and binding reconstruction but adds a component ledger between binding reconstruction and class output.

A component is retained only when it has source support, performs a distinct research function, passes an independent-variation test, is at the smallest complete natural class grain, and is non-duplicate after coreference/function reconciliation.

V43 also adds two paired controls:

1. **decompose meaning, not grammar** — nested/local semantic components are preserved when they do separate research work, even inside one broad clause/binding; and
2. **wrapper suppression** — broad discourse/report/thought/announcement wrappers are removed when their only job is to package content already represented elsewhere.

The same component logic controls compound reconstruction: distinct semantic edges may receive distinct compounds, while grammatical subsets/co-occurrences/wrappers do not.

These are cross-case rules. Worker-visible V43 instructions contain no archetype rows, expected counts, evaluator-specific findings, scored outputs, or Case 5 information.

## Historical integrity

`RI-CONTRACT-V42`, its task rules, adapters, runners, workflow-era history, and run `34996430918` remain unchanged historical evidence. V43 applies prospectively to new calibration only.

## Retained controls

- Both immutable archetype workbooks must pass the presidential SHA-256 gate before every use.
- Hidden evaluator findings never enter worker prompts.
- Calibration remains candidate-only.
- Mechanical validator feedback remains limited to deterministic mechanical defects.
- Every attempt/failure remains durable training history when possible.
- Sealed Case 5 remains closed until repeated Case 2 and Case 6 passes under one finalized V43 contract and saved-agent lineage.
- Holdout may run exactly once on that calibrated lineage.
- Clean-room certification requires a brand-new saved agent with finalized durable instructions only and no prior session/holdout output.
- No Oval Office promotion, APA-ID minting, or database admission.

## Current state

`RI-CONTRACT-V43`: `ACTIVE CALIBRATION SUCCESSOR — NOT CERTIFIED`.

No holdout authority has opened. Certification remains closed pending repeated Case 2/Case 6 archetype passes.
