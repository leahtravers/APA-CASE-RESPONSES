# V56 CALIBRATION CORRECTION RECORD

Status: ACTIVE ADDITIVE SUCCESSOR RECORD
Date: 2026-09-16
Successor contract: `RI-CONTRACT-V56`
Predecessor contract: `RI-CONTRACT-V55`
Preserved predecessor run: `35108881776`

## Finding

Preserved V55 workflow run `35108881776` passed the exact immutable Case 2 and Case 6 SHA-256 verification gate and the deterministic apparatus tests before semantic calibration. It executed under `RI-CONTRACT-V55`, preserved its failed outputs and session evidence, and did not reach the sealed holdout. The failure is therefore not attributable to workbook integrity, a current contract-version mismatch, or a missing deterministic test gate.

Across both approved archetypes V55 still produced a generalizable representation-grain failure. It admitted many surface lexical items that did not warrant primitive coordinate identity while omitting source-supported structural scene/episode/context anchors. It also showed cross-class type errors, over-combined and under-split lexical relations/orientations, value-grain errors, and compound divergence caused by the primitive graph mismatch.

The common failure is deeper than simple over- or under-inventory: V55 still treats independent lexical revisitability as too close to the primitive-admission criterion. The approved calibration behavior requires preservation of the source-supported **representation architecture**—nodes, frames, relation atoms, values, and orientation edges—even where some structural coordinates are unnamed or grammatically weak, while rejecting incidental lexical salience.

This is a generalizable agent-behavior defect because it occurs in both immutable calibration cases and across PLACE, TIME, OBJECT, LABEL, VERB, LOCATOR, and compound construction. No case-specific gold row is required to state the correction.

## Relationship to V53, V54, and V55

- V53 correctly resisted syntactic census but suppressed too many genuinely source-distinguished nested/structural coordinates.
- V54 restored breadth but treated semantic separability too much like primitive identity, producing over-decomposition.
- V55 distinguished coordinate identity from semantic separability, but its independence/revisitability framing still underweighted structural graph roles and over-weighted surface lexical candidates.

V56 retains all valid predecessor safeguards while changing the organizing question from “is this independently revisit-able wording?” to “what is the smallest source-supported coordinate set that preserves the represented graph?”

## V56 correction

V56:

1. reconstructs a source-supported representation graph before final class admission;
2. admits distinct structural PLACE/TIME anchors when they organize represented relation clusters, including source-supported unnamed anchors, without minting one frame per predicate;
3. classifies by represented function rather than lexical/part-of-speech shape;
4. treats VERBs as relation atoms, splitting or merging according to distinct graph edges rather than token or clause boundaries;
5. treats LOCATORs as orientation edges and preserves separately material path, position, accompaniment, containment, destination, origin, exit, proximity, or figurative orientation roles;
6. preserves LABEL value/polarity atoms at the smallest complete source value grain rather than either modifier census or whole-sentence swallowing;
7. retains stable mental/abstract/relation-result content as OBJECT when source-treated as a node, while rejecting incidental noun census;
8. performs a structural-completion pass to detect source-supported graph roles omitted by purely lexical extraction;
9. reconstructs compounds as material graph bindings among frozen primitives, not as lossless syntax or combinatorial closure;
10. retains exact-source literal/posture lock, evaluator isolation, immutable archetype verification, candidate-only status, and holdout protections.

## Isolation

No approved archetype rows, expected counts, evaluator answers, case-specific gold corrections, sealed holdout source, or holdout outputs are included in V56 instructions. Gold workbooks remain evaluator-side only.

## Historical integrity

V55 remains unchanged and authoritative historical evidence for work executed under it. V56 controls only new calibration/certification work. Existing calibration history is preserved; no failed attempt is deleted or relabeled.

## Holdout gate

Sealed Case 5 remains closed. V56 must first pass the immutable Case 2 and Case 6 hash gate repeatedly under one finalized saved-agent lineage. Only then may the existing one-shot sealed holdout workflow proceed. Certification still requires a second holdout success by a brand-new saved agent in a clean session with only finalized V56 durable instructions.
