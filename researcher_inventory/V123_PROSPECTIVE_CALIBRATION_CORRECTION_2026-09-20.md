# RESEARCHER INVENTORY V123 PROSPECTIVE CALIBRATION CORRECTION RECORD

Status: `PROSPECTIVE SUCCESSOR CORRECTION — CANDIDATE CALIBRATION ONLY`
Date: 2026-09-20
Authority: Leah's standing Researcher Inventory calibration instruction
Registrar record: `APA-EXEC-2026-09-20-CASES-RI-V123-CAL-0001`
Predecessor contract: `researcher_inventory/AGENT_CONTRACT_V122.md`
Successor contract: `researcher_inventory/AGENT_CONTRACT_V123.md`

## Preserved predecessor evidence

V122 remains preserved as historical calibration authority for the executions that used it.

Two separate paired-archetype V122 diagnostic runs reached the worker semantic gate after the same deterministic prerequisites:

- workflow run `35531362428-A1`, terminal `FAILED`, history preserved by commit `103eca26ef56a51da227fb8fb867ae73c037ef65`;
- workflow run `35534317557-A1`, terminal `FAILED`, history preserved by commit `bd6aededa62b7a43388e669f0dc873df689a39db`.

For both runs:

- Case 2 repository workbook matched SHA-256 `d47915552fdbdd23adc8c3f66ab06f94e022ab0bfd93faa5faabb53fdf9c9193`;
- Case 6 repository workbook matched SHA-256 `50d646aad08b33051b93c1f3b3538d59ff78386558473b71a906a94213635070`;
- no workbook repair was required;
- all 21 deterministic apparatus/harness tests passed;
- both archetypes failed the first semantic diagnostic batch;
- the sealed Case 5 holdout was not reached;
- no new evaluator/harness defect was established.

No predecessor contract, run, result, or failure evidence is deleted or rewritten.

## Generalizable failure class

Repeated V122 evidence supports one worker-semantic correction at a higher level than any case-specific answer:

1. **episode/support under-segmentation** — the worker can miss materially differentiated unnamed or source-established support frames even after being told to map bindings;
2. **surface-shape type attraction** — the worker can type a phrase from lexical appearance or ordinary denotation rather than from the role the source is making the unit perform in the represented episode;
3. **simultaneous over- and under-admission** — it can admit incidental literal scene/detail spans while omitting compact source-native role-bearing units;
4. **compound error is substantially downstream** — primitive ledger mismatch propagates into compound mismatch, so the next correction must repair primitive admission/type resolution before changing evaluator semantics.

These observations are recorded at the defect-class level only. Hidden archetype rows, expected counts, evaluator answers, and case-specific gold corrections are not placed into worker-facing instructions.

## Prospective correction

V123 requires a private **material episode ledger** before class extraction and makes that ledger the admission surface.

A primitive is prospectively admitted only when it is one source-native value filling a distinct source-established role in the episode ledger. Typing follows the role the source uses the unit to perform, not its surface lexical shape. PLACE/TIME support is derived from episode structure rather than place/time words. Explicit spans remain subject to a material-role test so literal addressability cannot become a census rule.

This preserves V122's valid principles:

- full-source reading;
- source-native literal lock;
- legitimate local/one-use coordinates;
- unnamed support frames;
- class-local identity;
- selective compounds after primitive freeze;
- worker isolation from archetypes/evaluator/holdout;
- immutable workbook verification;
- proven V66/V88A evaluator mechanics;
- attempt-history durability;
- one-shot holdout and clean-room gates;
- candidate-only storage and no APA-ID/promotion boundary.

## Harness disposition

`NO NEW HARNESS DEFECT ESTABLISHED.`

V123 reuses the proven evaluator, field-aware alignment, editor-shorthand alignment, immutable-archetype verifier, history-preservation mechanism, and holdout isolation. Harness semantics are not changed by this correction.

## Forward-control boundary

V123 controls only calibration executions launched after the V123 activation commit. V122 and earlier executions continue to be evaluated under the contracts that actually governed them.

The sealed Case 5 holdout remains inaccessible until V123 passes the full repeated Case 2 + Case 6 gate under one finalized contract and saved-agent lineage.
