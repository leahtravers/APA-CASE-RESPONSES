# RESEARCHER INVENTORY V22 CALIBRATION CORRECTION RECORD

Status: CALIBRATION HISTORY / FORWARD CORRECTION RECORD  
Date: 2026-09-13  
Authority: Leah's standing Researcher Inventory calibration directive  
Predecessor calibration contract: `AGENT_CONTRACT_V21.md` / `RI-CONTRACT-V21`  
Successor calibration contract: `AGENT_CONTRACT_V22.md` / `RI-CONTRACT-V22`  
Primary predecessor failed run: `34800603953` attempt `2`

## Immutable gold gate

Before this correction, preserved run `34800603953` verified both repository archetype workbooks before and after the integrity gate with no repair:

- `Case_2_Lightweight_Researcher_Inventory.xlsx` = `d47915552fdbdd23adc8c3f66ab06f94e022ab0bfd93faa5faabb53fdf9c9193`
- `Case_6_Lightweight_Researcher_Inventory_v2_with_Verbs_and_Locators.xlsx` = `50d646aad08b33051b93c1f3b3538d59ff78386558473b71a906a94213635070`

No gold workbook, canonical extract, expected count, evaluator pass criterion, or sealed holdout object is changed by V22.

## Preserved predecessor evidence

The complete V21 attempt remains preserved at:

`calibration_history/researcher_inventory/34800603953/`

Its status remains failed. Holdout stages were skipped. No sealed Case 5 source or output was committed or used for this correction.

## Defect separation

### A. Harness state — no new harness defect demonstrated

The immutable SHA-256 gate and deterministic apparatus tests passed. V21 uses the evaluator-only alignment repair admitted in V20. The current failure does not demonstrate a new evaluator-alignment, canonical-extract, workbook, source-copy validator, or deterministic harness defect.

Therefore V22 does not change:

- gold workbook bytes;
- canonical expected rows;
- expected counts;
- evaluator thresholds/pass criteria;
- V20 inflection/editor-shorthand alignment repair;
- source-copy validation;
- sealed-holdout mechanism.

### B. Worker behavior — durable successor required

V21 corrected V20 under-resolution by replacing global marginality pressure with class-local coordinate closure. Run `34800603953` attempt 2 demonstrates that V21 overshot in the opposite direction.

Across both archetypes the worker produced broad extra coordinate sets in PLACE, TIME, OBJECT, LABEL, VERB, and LOCATOR while still missing some approved coordinates. Excess unit grain then cascaded into compound disagreement. The pattern is cross-case and cross-class rather than tied to one phrase.

The generalizable defect is:

> V21 made materially represented content plus a plausible class job too close to a sufficient admission condition. Its `COVERAGE FIRST, PRUNING SECOND` verification could add incidental predicate, modifier, nested-relation, and micro-frame detail even when that detail did not deserve independent lightweight researcher-index status.

V20's predecessor evidence also remains material: a return to `omit if marginal` or global material-loss pressure would recreate under-resolution of valid local coordinates.

## V22 durable repair

V22 replaces both extremes with one stable sequence:

1. **ADMISSION FIRST** — each candidate must be source-grounded, perform its class job, have independent researcher-selectable/reconnective coordinate status at lightweight grain, and cause material semantic/navigation loss if omitted.
2. **BOUNDED CLOSURE SECOND** — scan the complete source for all additional candidates that independently pass the same gate, including valid local/single-episode coordinates and valid cross-class overlaps.
3. **PRUNE THIRD** — remove same-class aliases, true duplicate coordinates, grammar/support debris, invention, and candidates that fail independent coordinate status.

The worker must optimize for neither sparsity nor exhaustiveness. `Lightweight` is defined as semantic grain, not a target row count.

### Class-boundary tightening

V22 clarifies the general admission boundary without adding case-specific expected answers:

- PLACE requires an independently indexable where-coordinate, not every entity association, accompaniment, or spatially imaginable predicate.
- TIME requires an independently indexable episode/frame/span, not every action, micro-step, clause, or tense change.
- OBJECT requires an independently referable thing/relation/choice; propositions, predicates, and arbitrary nominalizations are not objects by default.
- LABEL requires an independently retrievable characterization, not every modifier or status clause.
- VERB requires an independently reconnectable semantic edge; syntactically nested predicates are not automatically separate edges.
- LOCATOR requires an independently indexable locating/context relation, not routine semantic argument marking or every spatial metaphor.
- compounds use only retained units that materially participate and only specific retained frames that actually govern the proposition.

These are domain-general requirements derived from the cross-case failure pattern. No archetype row, expected count, evaluator finding, or sealed holdout content is placed in worker-visible instructions.

## Verification behavior

The V22 adapter uses the same gate in both initial extraction and the stateless verification pass:

`ADMISSION AUDIT → BOUNDED CLOSURE → PRUNE/EXACTNESS`

The verifier is instructed not to prefer the provisional row count and not to widen or narrow admission merely because a row appeared or did not appear in the provisional output.

## Holdout boundary

Case 5 remains sealed. V22 must pass the immutable Case 2 and Case 6 archetypes repeatedly under the same finalized V22 contract and same saved-agent lineage before the calibrated-lineage holdout is permitted exactly once.

If that holdout passes, the calibrated lineage must be retired and a brand-new saved agent created from finalized V22 durable instructions only for a new-session clean-room Case 5 verification.

## Candidate-only boundary

All calibration outputs remain candidate/test material. V22 creates no APA identity, grants no promotion authority, authorizes no Oval Office research write, and authorizes no database mutation.

## Forward state

`RI-CONTRACT-V22` is authorized for calibration only. Certification remains contingent on repeated archetype passes, one calibrated-lineage sealed holdout pass, retirement, and fresh-agent clean-room holdout success.
