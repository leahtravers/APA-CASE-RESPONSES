name: Researcher Inventory Apparatus Verification
# V156 remains the current durable semantic calibration contract.
# V157 is a semantically-neutral harness successor: it reconciles the exact managed Agents root-turn lifecycle before accepting output.
# The retained V153A one-turn whole-source topology and all V156 researcher-handle relation phrase-grain instructions remain unchanged.
# Immutable archetype verification, evaluator mechanics, durable attempt history, one-shot holdout isolation, and clean-room certification remain mandatory.
# No semantic gold, expected counts, evaluator findings, case-specific hidden corrections, predecessor scored outputs, or holdout material are supplied to the worker.
# V157 harness routing trigger: 2026-09-22.

on:
  workflow_dispatch:
  schedule:
    - cron: '*/15 * * * *'
  push:
    branches: [main]
    paths:
      - 'researcher_inventory/**'
      - '.github/workflows/researcher-inventory-contract-test.yml'

permissions:
  contents: write

concurrency:
  group: researcher-inventory-apparatus-verification
  cancel-in-progress: ${{ github.event_name == 'push' }}

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 330
    env:
      OPENAI_API_KEY: ${{ secrets.CASES_INVENTORY_KEY }}
      OPENAI_MODEL: gpt-5.6-sol
      CONTRACT_VERSION: RI-CONTRACT-V156
      RI_CONTRACT_FILE: researcher_inventory/AGENT_CONTRACT_V156.md
      RI_MODEL_COMMAND: python researcher_inventory/v157_openai_agents_adapter.py
      RI_AGENT_SESSION_TIMEOUT_SECONDS: 600
      RI_AGENT_SESSION_TOTAL_TIMEOUT_SECONDS: 1800
      RI_AGENT_RESULT_WAIT_SECONDS: 120
    steps:
      - name: Checkout repository without external actions
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git init .
          git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git fetch --depth 1 origin main
          git checkout -B main FETCH_HEAD
          git config user.name "APA Cases Apparatus"
          git config user.email "apa-cases-apparatus@users.noreply.github.com"

      - name: Run V156 durable contract through retained V153A harness with V157 turn-aware recovery
        shell: bash
        run: |
          set -euo pipefail
          bash researcher_inventory/v157_pipeline.sh
